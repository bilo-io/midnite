// @midnite/ui — the reusable component library + design system.
//
// Phase 25 Theme A stands up the package and its Vite build. The generic
// primitives (button, card, input, select, …) migrate here from
// packages/web/components/ui in Theme C; until then the public surface is the
// shared styling helper every primitive is built on.
export { cn } from './lib/cn';
