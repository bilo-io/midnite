# Phase 73 — Admin Console & shared app shell

> midnite has every operator-facing surface it needs *as data* — [Phase 61](phase-61-fable-observability.md) gives real token/cost **usage attribution** + cycle-time metrics, [Phase 22](phase-22-fleet-visibility.md) the runtime **Ops** metrics, [Phase 33](phase-33-multi-user-teams.md) **users/teams/invites** + audit, [Phase 55](phase-55-projects-detail-page.md) the **projects** cockpit, and [Phase 71](phase-71-app-update-banner.md) the build-emitted **version manifest** + changelog + channels/floor. What's missing is a single **operator-facing app** that composes them: a standalone **`packages/admin`** Next.js console — platform usage, users, projects, versions, quick links — that **looks and feels exactly like `web`**. Phase 73 delivers it by first extracting the reusable app shell into a **new `@midnite/shell`** package (and the pure visuals into `@midnite/ui`), refactoring `web` to consume it so there's one source of truth, then standing up `admin` on the same shell behind a new **operator** identity — including a **lock screen** (idle re-lock **and** themed login) reusing the neuro-cloud **starfield/dots**.

> **Scope guardrails (CLAUDE.md).** This introduces **two new packages** and therefore **two new boundary edges** — get them right: `@midnite/ui` stays a **strict leaf** (only pure visuals move in; the `BackgroundPattern` union is **inlined** so `ui` still imports nothing in-repo, keeping [`boundary.test.ts`](../packages/ui/src/boundary.test.ts) green), and **`@midnite/shell`** is a new mid-tier package that **may depend on `shared` + `ui`** (with `next` + `@tanstack/react-query` as **peers**) but **never** on `web`/`admin`/`gateway` internals. `admin`, like `web`, is a **pure HTTP/WS client of the gateway** — it never imports gateway internals; cross-package types live in `shared`. **`shared` remains the config + contract authority.** The operator gate rides the [Phase 72](phase-72-sso-go-live-operator-config.md) operator-config seam — no new persisted global-role model. Versions is **view-only** — no new gateway write API.

> Effort tags: **S** small · **M** medium · **L** large. Critical path is **A → B → C** (extract visuals → build the shell → refactor `web` onto it, proving the shell is real and web is unbroken) then **D → E** (operator gate → scaffold `admin` on the shell); **F** (the admin sections) is the payload and **G** (tests/docs/boundaries) hardens. A+B+C is "the shell exists and web still works"; D+E is "the admin app boots, gated"; F is "operators can actually see everything".

---

## Current state (what exists to build on)

- **`@midnite/ui` is a confirmed leaf** — [`src/boundary.test.ts`](../packages/ui/src/boundary.test.ts) greps shipped source and **fails on any `@midnite/(shared|web|gateway|cli|desktop|site)` import**; `react`/`react-dom` are **peers**. It already ships the **design tokens + `.dark`** ([`@midnite/ui/styles`](../packages/ui/src/index.ts)), the **theme runtime** (`ThemeProvider`/`useTheme`/FOUC script at `@midnite/ui/theme`), `cn`, and primitives (Button/Card/Input/Select/Switch/Tabs/…). **No shell-level component exists there yet.**
- **The visuals to reuse are pure** — [`neuro-cloud-background.tsx`](../packages/web/components/neuro-cloud-background.tsx) (the starfield / gather-release canvas) imports **only `react` + `cn`**; [`rail-shell.tsx`](../packages/web/components/rail-shell.tsx) (nav-rail chrome) imports **only `react` + `lucide-react` + `cn`**; [`dynamic-background.tsx`](../packages/web/components/dynamic-background.tsx) imports `react` + `cn` + a **type-only** `BackgroundPattern` from `@midnite/shared`. "Dots" is not a component — it's a `bg-dots` CSS utility in web `globals.css`, mapped via `BACKGROUND_PATTERN_CLASS` in [`app-settings.ts`](../packages/web/lib/app-settings.ts).
- **The wired shell is data-coupled** — [`nav-bar.tsx`](../packages/web/components/nav-bar.tsx) drives its items from the typed `FEATURES` registry in [`lib/features.ts`](../packages/web/lib/features.ts) (grouped, feature-flag-filtered), is route-aware via `usePathname`, and pulls in `Screensaver`, presence, WS `connection-store`, `ThemeToggle`, `PasscodeSetupDialog`. [`screensaver.tsx`](../packages/web/components/screensaver.tsx) is a **container** (`@midnite/shared` `SessionStatus`, `lib/api` `getSessions`, telemetry hooks, `NeuroCloudBackground`, `PasscodeUnlockDialog`). The Phase 39/68 **accent/gradient runtime** lives in [`lib/apply-appearance.ts`](../packages/web/lib/apply-appearance.ts) + [`app-settings.ts`](../packages/web/lib/app-settings.ts) (depends on `@midnite/shared` types). None of these can live in leaf `ui`.
- **The web app frame** — [`app/layout.tsx`](../packages/web/app/layout.tsx) mounts fonts + two FOUC init scripts + `AppearanceEffects`, `ThemeProvider`, `AuthProvider`, `ToastProvider`, `ConfirmProvider`, `UpdateProvider`+`UpdateBanner`, `PwaRegister`; [`app/(main)/layout.tsx`](../packages/web/app/(main)/layout.tsx) mounts `QueryClientProvider` → `NotificationsProvider` → `PaletteCommandsProvider`, `AuthGuard`, `LiveData` (WS), `PreferenceSync`, `NavBar`, and more. A second app must re-stand-up a compatible frame.
- **Read-ready gateway APIs** — usage: `GET /usage/summary`, `GET /usage/attribution`; metrics: `GET /metrics/ops|cycle-time|runs|rollups`; audit: `GET /audit`; teams: `GET/POST/PATCH/DELETE /teams…` + invites; projects: `GET /projects…`. **All consumable as-is.**
- **The two real gaps** — (1) **no cross-user/cross-team "list all" admin endpoint** ([`users.controller.ts`](../packages/gateway/src/users/users.controller.ts) is self-only `GET/PATCH /users/me`; [`admin.controller.ts`](../packages/gateway/src/admin/admin.controller.ts) is just `POST /admin/backup`); (2) **no global operator role** — `TeamRole` (`owner/admin/member/viewer`, [`shared/src/team.ts`](../packages/shared/src/team.ts)) is **team-scoped only**, enforced by [`role.guard.ts`](../packages/gateway/src/auth/role.guard.ts); usage/metrics/audit carry **no** role guard, and static-token/anon installs bypass role checks entirely. Version/changelog is a **static** web-served `version.json` polled by [`lib/version.ts`](../packages/web/lib/version.ts) — **not** a gateway API.
- **Operator-config seam (Phase 72)** — [Phase 72](phase-72-sso-go-live-operator-config.md) carves `gateway.auth` into a gitignored **`.midnite/operator.json`** via a reusable `OperatorConfigSchema` (deep-merged in [`config-loader.ts`](../packages/shared/src/config-loader.ts)). Phase 73's operator allowlist rides the **same file** — the natural home for "who is an operator".

---

## Theme A — `@midnite/ui` leaf-visual extraction — **M**

Move the genuinely pure visuals into the design-system leaf, so both apps share one starfield/chrome without breaking the leaf rule.

- [ ] **Move `NeuroCloudBackground`** (the starfield / neuro-cloud gather-release canvas) into `@midnite/ui` (e.g. `packages/ui/src/backgrounds/neuro-cloud-background.tsx`) and export it from [`src/index.ts`](../packages/ui/src/index.ts). It already depends on nothing but `react` + `cn` (which `ui` provides). Web keeps a **re-export shim** at [`components/neuro-cloud-background.tsx`](../packages/web/components/neuro-cloud-background.tsx) so existing imports (incl. `screensaver.tsx`) are untouched.
- [ ] **Move `DynamicBackground`** into `ui`, **inlining the `BackgroundPattern` union** (or a local structural equal) so it stops importing `@midnite/shared` even type-only — the leaf rule forbids it. Keep the shared `BackgroundPattern` as the contract; `ui`'s copy is a small duplicated string-union with a comment pointing at the source of truth (the same "copy, don't cross the boundary" pattern the site uses for favicons).
- [ ] **Move `RailShell`** (the presentational nav-rail chrome, `react` + `lucide-react` + `cn`) into `ui` as the reusable rail primitive the shell's `<AppFrame>` (Theme B) composes.
- [ ] **Optionally move `ThemeToggle` + `PasscodePad`** into `ui` if they stay leaf-safe (ThemeToggle already consumes `ui`'s `useTheme` + Button; `PasscodePad` is a self-contained keypad). If either needs shared/data, leave it for the shell (Theme B) instead — don't force it into the leaf.
- [ ] **Keep `boundary.test.ts` green** — re-run [`ui:test`](../packages/ui/src/boundary.test.ts) after each move; the grep must still find **zero** in-repo imports in shipped `ui` source. Add the moved components to `ui`'s Storybook/exports as appropriate.

## Theme B — `@midnite/shell` package (`<AppFrame>` + lock + appearance + providers) — **L**

A new mid-tier package that owns the *wired* app frame, so `web` and `admin` mount the same shell instead of duplicating it.

- [ ] **Scaffold `packages/shell`** (`@midnite/shell`) — `package.json` (deps: `@midnite/ui`, `@midnite/shared`, `clsx`/`tailwind-merge` via `ui`; **peers**: `react`, `react-dom`, `next`, `@tanstack/react-query`), `moon.yml` with `dependsOn: [ui, shared]`, `tsconfig`, Vite/tsup **library** build mirroring `@midnite/ui`, and a **`boundary.test.ts`** that fails on any import of `@midnite/(web|admin|gateway|cli|desktop|site)` (shell may import `shared` + `ui` **only**).
- [ ] **`<AppFrame>`** — a configurable frame that takes an **injected nav config** (`{ sections: NavSection[], brand, footer? }`) plus `slots` (header actions, banner) and renders the `RailShell`-based sidenav (route-aware via an injected `usePathname`/active-path prop so shell stays router-agnostic), header, and content region. Nav items come from the caller, **not** a hardcoded `FEATURES` list — that's the seam that lets `web` and `admin` differ.
- [ ] **Lock screen** — port [`screensaver.tsx`](../packages/web/components/screensaver.tsx) into shell as a reusable `<LockScreen>` composing the `ui` `NeuroCloudBackground` + `PasscodeUnlockDialog`, parameterised over its data source (sessions/telemetry provided by the host app via props/hooks, not a hard `lib/api` import) — so both the **idle re-lock** and the **themed login gate** (Theme E) render on the starfield. Idle-timeout + passcode behaviour matches web's screen-lock settings.
- [ ] **Appearance/accent runtime** — move the Phase 39/68 appliers ([`apply-appearance.ts`](../packages/web/lib/apply-appearance.ts): `applyAccent`, `accentGradientCss`, `applyBackground`, …) + the accent/gradient/background-pattern **CSS layer** (the ~200 `data-accent`/gradient rules currently in web `globals.css`) into shell (a shell-owned `appearance.css` + `apply-appearance.ts`), driven off the `shared` `UserPreferences`/appearance types. Both apps import the one appearance stylesheet + applier.
- [ ] **Provider composition** — a `<ShellProviders>` (or documented composition) bundling the frame-level providers both apps need: `ThemeProvider` (from `ui`), `QueryClientProvider` (host supplies the client), Auth context, Notifications, WS/live-data, PreferenceSync. Auth/WS/preferences read `shared` contracts + a host-supplied API client (injected), keeping shell gateway-client-agnostic.
- [ ] **Public entry + tests** — `src/index.ts` exports `AppFrame`, `LockScreen`, `ShellProviders`, appearance helpers, and nav-config types; unit tests over the nav-config → rendered-rail mapping, the lock idle-timer, and the appearance applier. Update **[`CLAUDE.md`](../CLAUDE.md)** package-boundary graph + **[`.moon/workspace.yml`](../.moon/workspace.yml)** to register the package.

## Theme C — Refactor `web` onto `<AppFrame>` — **M**

Prove the shell is real by making the shipping web app consume it — **behaviour-preserving**, tests green.

- [ ] **Mount `<AppFrame navConfig={…}>`** in [`app/(main)/layout.tsx`](../packages/web/app/(main)/layout.tsx), feeding web's existing `FEATURES`/`groupNavSections` ([`lib/features.ts`](../packages/web/lib/features.ts)) as the injected nav config (incl. its feature-flag filtering). Web-specific extras (`PresenceNavPill`, `ConnectionToaster`, `AssistantFab`, command palette) stay web-owned, passed in as `slots` or mounted alongside.
- [ ] **Source appearance + lock from shell** — web's [`appearance-effects.tsx`](../packages/web/components/appearance-effects.tsx) and the idle `Screensaver` now use shell's appearance runtime + `<LockScreen>`; delete the migrated CSS/appliers from web `globals.css`/`lib` (or leave thin shims) so there's **one** source. Web's [`preference-sync.tsx`](../packages/web/components/preference-sync.tsx) uses shell's provider.
- [ ] **Re-export shims stay** for anything moved (Theme A/B) so unrelated web imports don't churn; the diff is "compose the frame", not "rewrite pages".
- [ ] **No visual/behaviour regression** — theme toggle, accent/gradient, background patterns, idle lock, nav grouping, mobile nav all behave exactly as before. Re-run [`web:test`](../packages/web) (unit + stories) + a manual/Playwright pass on the board, settings→appearance, and the screen-lock. This is the acceptance gate for the extraction.

## Theme D — Operator identity & platform-scoped read APIs — **M**

A minimal global **operator** gate (no new persisted role model) + the handful of admin-only read endpoints the console needs.

- [ ] **Operator allowlist in operator-config** — extend Phase 72's `OperatorConfigSchema` ([`shared/src/config.ts`](../packages/shared/src/config.ts)) with an `operators` list (emails and/or SSO subject IDs). It lives in the gitignored **`.midnite/operator.json`** — operator policy, exactly like the SSO allowlist. Absent ⇒ no operators (fail-closed to "no admin access"), never "everyone".
- [ ] **`isOperator` claim + `@RequiresOperator` guard** — resolve `isOperator` for the authenticated principal against the allowlist (in the auth/JWT layer, alongside [`role.guard.ts`](../packages/gateway/src/auth/role.guard.ts)); a new `@RequiresOperator()` decorator + guard gates operator-only routes. Reuse Phase 33 JWT identity; single-user/static-token installs are **not** operators unless explicitly allowlisted (closes the current bypass for these new routes).
- [ ] **Platform-scoped read endpoints** — new **operator-gated** routes for the cross-tenant reads the console needs: `GET /admin/users` (list all users), `GET /admin/teams` (list all teams), and a `GET /admin/overview` aggregate (platform KPIs: user/team/project/task counts, active sessions, rollup usage+cost) composed from existing services. Thin controllers → services → existing repositories; **no** new tables. DTOs in `shared`.
- [ ] **Leave existing team-scoped routes untouched** — do **not** retro-gate `/usage`, `/metrics`, `/audit` (web's Ops page depends on them); admin's platform-wide views use the new `@RequiresOperator` aggregates instead. This is additive, not a tightening that could break `web`.
- [ ] **Contract + client** — add the admin DTOs (`AdminUserSummary`, `AdminTeamSummary`, `PlatformOverview`) and typed client methods to `shared`'s API client; tests for the operator guard (allowlisted ⇒ 200, non-operator ⇒ 403, anon ⇒ 401) and the aggregate shapes.

## Theme E — `packages/admin` app scaffold + shell mount — **M**

Stand up the standalone console on the shared shell, gated to operators, visually identical to web.

- [ ] **Scaffold `packages/admin`** — a Next.js App Router app (`package.json`, `moon.yml` `dependsOn: [shell, ui, shared]`, `tsconfig`, `next.config.mjs`, Tailwind wired to `@midnite/ui/styles` + shell `appearance.css`), fonts + FOUC theme/appearance init scripts mirroring [web's `app/layout.tsx`](../packages/web/app/layout.tsx). A **client-only** app (talks to the gateway over HTTP/WS like `web`), **not** bundled into desktop.
- [ ] **Mount `<AppFrame>` + `<ShellProviders>`** with an **admin nav config** (Overview / Usage / Users & teams / Projects / Versions / Audit / Links) — the same rail chrome, theme system, and appearance runtime as web, so it looks native from the first render.
- [ ] **Operator login gate + idle lock** — the app is gated behind the **themed login** (Phase 70 SSO + the operator allowlist check) rendered on the starfield `<LockScreen>`; a non-operator who authenticates sees a clean "not an operator" state, not a broken app. The **idle re-lock** (passcode) reuses the same `<LockScreen>`. Both use the neuro-cloud starfield/dots — the lock experience the seed asked for.
- [ ] **Gateway client config** — admin reads `NEXT_PUBLIC_GATEWAY_URL` (same pattern as web's server target) for the API/WS origin; a minimal `lib/api.ts` over `shared`'s typed client. **Settings**: admin gets only the appearance + screen-lock settings it needs (reuse shell/appearance; the full settings framework stays web-owned per scope).
- [ ] **Build + CI** — `admin:build`/`dev`/`test`/`lint`/`typecheck` moon tasks; register in [`.moon/workspace.yml`](../.moon/workspace.yml); confirm the affected-graph builds `ui`→`shell`→`admin` in order.

## Theme F — Admin sections (the operator console payload) — **L**

The pages themselves — mostly a **read-layer** over existing + Theme D endpoints, composed in shell chrome.

- [ ] **Overview** — a platform dashboard: KPI tiles (users, teams, projects, tasks by status, active sessions) + headline **usage/cost** (Phase 61) + recent activity, from `GET /admin/overview`. The operator's landing page.
- [ ] **Usage & cost** — compose Phase 61 `GET /usage/summary` + `GET /usage/attribution` (by user/team/project/session) + Phase 22 `GET /metrics/ops|cycle-time` into a drill-downable usage view; reuse `web`'s chart components where they've moved to `ui`, else lightweight equivalents. Time-range + dimension filters.
- [ ] **Users & teams** — list **all** users (`GET /admin/users`) and **all** teams (`GET /admin/teams`) with membership/roles; **team CRUD + role management** via the existing Phase 33/35 `/teams…` endpoints (create/rename/delete team, change member role, remove member). Per-user drill-down (identity, teams, last activity).
- [ ] **Projects** — list + inspect projects via Phase 55 `GET /projects…` (progress, task counts, linked repos); read-focused (management stays in web's project cockpit unless trivial).
- [ ] **Versions & releases (view-only)** — current running version + released versions from the Phase 71 `version.json`/`version.beta.json` manifests, the rendered **CHANGELOG**, and **channels + force-update floor read-only**. **No** write API (that's out of scope) — reads the existing static manifest + [`CHANGELOG.md`](../CHANGELOG.md).
- [ ] **Audit log** — the Phase 33/35 `GET /audit` viewer (filters: entity/user/action/date, paginated) surfaced for operators.
- [ ] **Quick links** — a links panel out to the **website**, **web app**, and **docs** (reuse/extend [`lib/site-links.ts`](../packages/web/lib/site-links.ts), moved to `shared` or shell if shared across apps), plus in-app deep links (Ops, board). The small, high-value seed ask.

## Theme G — Hardening: tests, docs & boundary enforcement — **M**

- [ ] **Boundary tests all green** — `ui` leaf test (Theme A), the **new `shell` boundary test** (Theme B: shell imports only `shared`+`ui`), and `admin`'s "never imports gateway internals" check; confirm CI runs all three. Update the enforcement note in [`CLAUDE.md`](../CLAUDE.md) to document the `ui ◀ shell ◀ {web, admin}` and `shared/ui ◀ admin` edges.
- [ ] **App tests** — `admin` unit/RTL tests for the nav config → rail render, the operator-gate redirect (operator ⇒ app, non-operator ⇒ blocked), and each section's happy-path render against mocked endpoints; Storybook stories for the new `shell`/`ui` components run as browser tests.
- [ ] **Operator-gate tests (gateway)** — allowlist resolution + `@RequiresOperator` (200/403/401), plus the aggregate endpoint shapes; the existing team-scoped routes' specs stay **unedited** (proof they weren't tightened).
- [ ] **Docs** — a `docs/ADMIN.md` (what the console is, the operator allowlist in `.midnite/operator.json`, how to run `admin` locally + hosted) and a short `@midnite/shell` package README (what's in the shell, how a third app would consume `<AppFrame>`); note the versions surface is view-only. Link from the README.
- [ ] **Green gates** — `moon run :typecheck && :lint && :test` across the expanded graph; `ui`/`shell`/`admin`/`web` all build; web behaviour unchanged (Theme C acceptance holds).

---

## Files this phase touches

| Area | Files |
|------|-------|
| ui · visuals | **move** [`neuro-cloud-background.tsx`](../packages/web/components/neuro-cloud-background.tsx), [`dynamic-background.tsx`](../packages/web/components/dynamic-background.tsx), [`rail-shell.tsx`](../packages/web/components/rail-shell.tsx) → `packages/ui/src/…` + [`src/index.ts`](../packages/ui/src/index.ts); [`src/boundary.test.ts`](../packages/ui/src/boundary.test.ts) stays green; web re-export shims |
| shell · new pkg | **new** `packages/shell/` — `package.json`, `moon.yml`, `src/index.ts`, `src/app-frame.tsx`, `src/lock/` (from [`screensaver.tsx`](../packages/web/components/screensaver.tsx)/[`passcode-pad.tsx`](../packages/web/components/passcode-pad.tsx)), `src/appearance/` (from [`apply-appearance.ts`](../packages/web/lib/apply-appearance.ts) + accent CSS), `src/providers/`, `src/boundary.test.ts` |
| web · refactor | [`app/(main)/layout.tsx`](../packages/web/app/(main)/layout.tsx) (mount `<AppFrame>` w/ `FEATURES`), [`app/layout.tsx`](../packages/web/app/layout.tsx), [`components/appearance-effects.tsx`](../packages/web/components/appearance-effects.tsx), [`components/preference-sync.tsx`](../packages/web/components/preference-sync.tsx), [`app/globals.css`](../packages/web/app/globals.css) (drop migrated accent CSS), [`lib/features.ts`](../packages/web/lib/features.ts) |
| gateway · operator | [`shared/src/config.ts`](../packages/shared/src/config.ts) (`operators` in `OperatorConfigSchema`), new `auth/operator.guard.ts` + `auth/decorators/require-operator.decorator.ts`, new `admin` routes (`GET /admin/users|teams|overview`) in [`admin.controller.ts`](../packages/gateway/src/admin/admin.controller.ts)/service, reuse [`users`](../packages/gateway/src/users)/[`teams`](../packages/gateway/src/teams)/[`projects`](../packages/gateway/src/projects)/`usage`/`metrics`/`audit` services |
| shared · contract | new `AdminUserSummary`/`AdminTeamSummary`/`PlatformOverview` DTOs + API-client methods; `site-links` shared home (if lifted) |
| admin · new app | **new** `packages/admin/` — `app/` (App Router, layout + sections), `moon.yml`, `package.json`, `next.config.mjs`, `lib/api.ts`, `tailwind`/`globals.css` wired to `@midnite/ui/styles` + shell appearance |
| workspace/docs | [`.moon/workspace.yml`](../.moon/workspace.yml) (register `shell` + `admin`), [`CLAUDE.md`](../CLAUDE.md) (boundary graph), **new** `docs/ADMIN.md`, `packages/shell/README.md`, [`done.md`](done.md) as items land |

---

## Verification

- [ ] **The shell is real and web is unbroken:** `web` mounts `<AppFrame>` from `@midnite/shell`; theme toggle, accent/gradient, background patterns, idle lock, nav grouping, and mobile nav all behave **exactly as before** (manual/Playwright pass on board + settings→appearance + screen-lock); `moon run web:test` green with no behavioural edits to page specs.
- [ ] **Leaf + boundary rules hold:** `moon run ui:test` (leaf) green — `ui` still imports nothing in-repo (the `BackgroundPattern` union is inlined); the **new** `shell` boundary test passes (imports only `shared`+`ui`); `admin` imports no gateway internals. `CLAUDE.md`'s graph documents `ui ◀ shell ◀ {web, admin}`.
- [ ] **`admin` boots on the shared shell:** `moon run admin:dev` renders the console with the **same** sidenav/theme/appearance as web; the neuro-cloud **starfield/dots** back both the **login gate** and the **idle re-lock**; a themed passcode/SSO lock works.
- [ ] **Operator gate works:** an allowlisted operator (`operators` in `.midnite/operator.json`) reaches the console; a non-operator who authenticates is cleanly blocked; anonymous is redirected to login. `GET /admin/users|teams|overview` return **403** for non-operators, **401** for anon, data for operators. Existing `/usage`/`/metrics`/`/audit` specs pass **unedited** (not retro-gated).
- [ ] **The sections show real data:** Overview KPIs + usage/cost render from live gateway data; Users & teams list **all** tenants with working team CRUD/role management; Projects list from Phase 55; **Versions is view-only** (released versions + rendered CHANGELOG + channels/floor read-only, no write API); Audit log filters; Quick links open website/webapp/docs.
- [ ] `moon run :typecheck && :lint && :test` green across the expanded graph; `ui`/`shell`/`admin`/`web` all build.

---

## Decisions / open questions

1. **Separate app vs. section of web** → **Resolved (user): a standalone `packages/admin` app**, not a `/admin` route group — with the shell extracted so both apps genuinely share it.
2. **Where the reusable shell lives** → **Resolved (user): a new `@midnite/shell` package** (Standard depth). Pure visuals go to leaf `@midnite/ui`; the wired `<AppFrame>` + lock + appearance runtime + providers go to `shell` (which may depend on `shared`+`ui`). The wired shell **cannot** live in leaf `ui` — importing `shared`/data would break its boundary test. `web` is refactored to consume `<AppFrame>` so there's one source of truth.
3. **Shell depth** → **Resolved (user): Standard** — `<AppFrame>` (injected nav config) + lock screen + appearance runtime + provider stack. The **settings framework stays web-owned** (admin reuses only appearance + screen-lock), keeping the web refactor bounded.
4. **Operator gate** → **Resolved (user): an operator allowlist in the Phase 72 operator-config** (`.midnite/operator.json`), deriving an `isOperator` claim + `@RequiresOperator` guard. **No** new persisted global-role model. Soft-depends on Phase 72's `OperatorConfigSchema`; if 72 hasn't landed, Phase 73 introduces the minimal operator-config read path it needs.
5. **Existing usage/metrics/audit gating** → **Recommendation: leave them as-is.** Admin's platform-wide views use **new** operator-gated aggregate endpoints so we don't risk breaking web's Ops page (which reads the ungated team-scoped routes). *(Recommended; confirm in review.)*
6. **Versions scope** → **Resolved (user): view-only** — released versions + changelog + channels/floor read-only + quick links. Managing channels/floor or publishing releases (needs a new gateway version-manifest **write** API) is **out of scope**.
7. **Lock screen** → **Resolved (user): both** an idle+passcode re-lock **and** a themed login gate, both on the neuro-cloud starfield/dots, housed in `@midnite/shell`.
8. **Admin sections** → **Resolved (user): Core + Audit + Teams management** — Overview, Usage & cost, Users & teams (list + team CRUD/role mgmt), Projects, Versions (view-only), Audit log, Quick links. Spend caps (Phase 50) + service tokens (Phase 38) mirrors are **out of scope** for this phase (a later widen).
9. **Admin login mechanism** → **Recommendation: reuse Phase 70 SSO** for the login gate + the operator allowlist check; passcode only for the idle re-lock. *(Recommended; confirm in review.)*
10. **Is `admin` packaged into desktop?** → **Recommendation: no** — a web-only standalone deployable for now (like a hosted operator console), not bundled into the Electron app. *(Recommended; confirm in review.)*
11. **`site-links` home** → if quick links are shared by `web` + `admin`, lift [`lib/site-links.ts`](../packages/web/lib/site-links.ts) into `shared` (or `shell`); otherwise duplicate the small list. *(Lean shared.)*
